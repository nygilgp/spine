{
  "mcpServers": {
    "billing": {
      "command": "npx",
      "args": [
        "-y",
        "billing-mcp"
      ],
      "env": {
        "BILLING_TOKEN": "${BILLING_TOKEN}"
      }
    }
  }
}